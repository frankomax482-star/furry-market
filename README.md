# FURRY MARKET 🦊

Полнофункциональный маркетплей для фурри-товаров, вдохновленный OZON

## ✨ Основные функции

### 👤 Для пользователей
- ✅ Регистрация/вход с номером или Google
- ✅ Личный кабинет с историей заказов
- ✅ Каталог товаров с фильтрацией
- ✅ Поиск по товарам
- ✅ Фильтры по цене, рейтингу, категории
- ✅ Темная/светлая тема
- ✅ Корзина покупок
- ✅ Оплата: СБП, карта, наличные, по ссылке
- ✅ Доставка: онлайн, офлайн, пункт выдачи
- ✅ Применение промокодов
- ✅ Отслеживание заказов

### 🔐 Для администраторов
- ✅ Админ панель
- ✅ Добавление/удаление/редактирование товаров
- ✅ Управление категориями
- ✅ Управление администраторами
- ✅ Создание и управление промокодами
- ✅ Просмотр статистики
- ✅ Управление заказами

## 🛠️ Технический стэк

### Backend
- **Node.js + Express** - REST API
- **MongoDB** - база данных
- **JWT** - аутентификация
- **Bcryptjs** - хеширование паролей
- **CORS** - кросс-доменные запросы

### Frontend
- **Next.js 13** - React фреймворк
- **Zustand** - управление состоянием
- **Tailwind CSS** - стилизация
- **React Icons** - иконки
- **Axios** - HTTP клиент
- **React Hot Toast** - уведомления

## 📦 Установка

### 1. Backend

```bash
cd backend
npm install
```

**Создайте файл `.env`:**
```env
PORT=5000
MONGODB_URL=mongodb://localhost:27017/furry-market
JWT_SECRET=your_secret_key_here
NODE_ENV=development
```

**Запуск:**
```bash
npm run dev
```

### 2. Frontend

```bash
cd frontend
npm install
```

**Запуск:**
```bash
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## 📂 Структура проекта

```
.
├── backend/
│   ├── src/
│   │   ├── models/          # MongoDB models
│   │   ├── routes/          # API routes
│   │   ├── middleware/      # Auth middleware
│   │   ├── controllers/     # Business logic
│   │   └── server.js        # Entry point
│   ├── package.json
│   └── .env.example
│
└── frontend/
    ├── src/
    │   ├── pages/           # Next.js pages
    │   ├── components/      # React components
    │   ├── utils/           # API & utilities
    │   ├── context/         # Context providers
    │   └── styles/          # CSS files
    ├── package.json
    └── next.config.js
```

## 🔌 API Endpoints

### Authentication
- `POST /api/auth/register/phone` - Регистрация с номером
- `POST /api/auth/login/phone` - Вход с номером
- `POST /api/auth/auth/google` - Google OAuth
- `GET /api/auth/profile` - Получить профиль

### Products
- `GET /api/products` - Список товаров с фильтрацией
- `GET /api/products/:id` - Получить товар
- `GET /api/products/category/:categoryId` - Товары по категории
- `GET /api/products/recommended/top` - Популярные товары

### Categories
- `GET /api/categories` - Все категории
- `GET /api/categories/:id` - Категория с подкатегориями

### Orders
- `POST /api/orders` - Создать заказ
- `GET /api/orders` - Мои заказы
- `GET /api/orders/:id` - Получить заказ
- `POST /api/orders/validate-promo` - Проверить промокод

### Admin
- `POST /admin/products` - Добавить товар
- `PUT /admin/products/:id` - Редактировать товар
- `DELETE /admin/products/:id` - Удалить товар
- `POST /admin/categories` - Добавить категорию
- `POST /admin/admins` - Добавить администратора
- `POST /admin/promos` - Создать промокод
- `GET /admin/stats/dashboard` - Статистика

## 🔐 Аутентификация

Система использует JWT токены, сохраненные в localStorage:

```javascript
// Получение токена
const token = localStorage.getItem('token');

// Отправка в header
Authorization: Bearer TOKEN
```

## 📝 Примеры использования

### Регистрация
```javascript
POST /api/auth/register/phone
{
  "phone": "+7 (999) 999-99-99",
  "firstName": "Иван",
  "lastName": "Петров",
  "password": "securePassword123",
  "confirmPassword": "securePassword123"
}
```

### Создание заказа
```javascript
POST /api/orders
{
  "items": [
    { "product": "123", "quantity": 2, "price": 1000 }
  ],
  "shippingAddress": {
    "street": "ул. Пушкина",
    "city": "Москва",
    "phone": "+7999999999"
  },
  "deliveryType": "online",
  "paymentMethod": "sbp"
}
```

## 🎨 Дизайн

### Цветовая схема
- **Primary**: Orange (#FF6B35)
- **Secondary**: Blue (#004E89)
- **Dark**: #1a1a1a
- **Light**: #f5f5f5

### Темы
- Светлая тема (по умолчанию)
- Темная тема (автоматическое переключение)

## 📱 Адаптивность

Проект полностью адаптивен:
- 📱 Мобильные устройства (320px+)
- 📱 Планшеты (768px+)
- 💻 Десктопы (1024px+)
- 🖥️ Большие экраны (1280px+)

## 🚀 Производство

### Backend
```bash
npm run build
npm start
```

### Frontend
```bash
npm run build
npm start
```

## 🐛 Частые ошибки

### MongoDB не подключается
- Убедитесь, что MongoDB запущен: `mongod`
- Проверьте URL в `.env`

### CORS ошибка
- Убедитесь, что frontend отправляет запросы на правильный URL backend

### Token истек
- Используйте endpoint refresh для обновления токена

## 📞 Поддержка

Для вопросов и багов создайте issue в репозитории.

## 📄 Лицензия

MIT License - используйте свободно!

---

Made with ❤️ for the furry community 🦊
